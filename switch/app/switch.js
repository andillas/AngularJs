var switchApp = angular.module('switchApp', []);
switchApp.controller('switchCtrl', function($scope){
    $scope.title_app = 'Pruebas con sentencias switch (ng-switch)';
    $scope.is_disabled = false;

});